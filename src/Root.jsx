/**
 * Internal dependencies
 */
import { Dashboard } from '@/pages';
import { Navigation } from '@/components';

const Root = () => (
	<>
		<Navigation />
		<Dashboard />
	</>
);

export default Root;

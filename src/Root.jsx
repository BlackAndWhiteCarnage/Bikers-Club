import { Navigation } from '@/components';
import { Home, RecommendedPosts } from '@/sections';

const Root = () => (
	<div>
		<Navigation />
		<Home />
		<RecommendedPosts />
	</div>
);

export default Root;
